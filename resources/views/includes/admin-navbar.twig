<nav class="navbar navbar-expand-lg navbar-dark bg-red fixed-top">
    <div class="container">
        <a class="navbar-brand logo-sect" href="/">Chrystacripsy</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ url_for('admin.index') }}">Dashboard</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ url_for('profile') }}">profile</a>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    Menus
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item" href="{{ url_for('add-menu') }}">Add a Menu</a>
                    <a class="dropdown-item" href="{{ url_for('menu-list') }}">View All Menus</a>
                    <a class="dropdown-item" href="{{ url_for('add-category') }}">Categories</a>
                    </div>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ url_for('all-users') }}">Users</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ url_for('orders') }}">Orders</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ url_for('delivery-locations') }}">Delivery Locations</a>
                </li>
                {# <li class="nav-item">
                    <a class="nav-link" href="{{ url_for('report') }}">Reports</a>
                </li> #}
            </ul>
            <ul class="navbar-nav ml-auto">
                <li class="nav-item active navbar-app">
                    <a class="nav-link" href="#"> Hi, Admin {{ user.first_name }}</a>
                </li>
                <li class="nav-item active navbar-app">
                    <form class="form-inline mr-3 my-2 navbar-app my-lg-0"action="{{ url_for('logout') }}" method="POST">
                        <button type="submit" class="btn btn-light">Log Out</button>
                        {{ csrf | raw }}
                    </form>
                </li>
            </ul>
        </div>
    </div>
</nav>